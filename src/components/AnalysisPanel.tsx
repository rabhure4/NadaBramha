import React from 'react';

export function AnalysisPanel() {
  return (
    <div className="h-full flex items-center justify-center text-[13px] text-muted italic">
      Analysis has been folded into the local diarization workflow in this build.
    </div>
  );
}
